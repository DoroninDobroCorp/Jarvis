# Copy this file to .env.local and fill in your Google Custom Search credentials
# Vite will expose variables starting with VITE_ to the client.

# API key for Google Custom Search API
VITE_GOOGLE_CSE_KEY=

# Custom Search Engine ID (CX)
VITE_GOOGLE_CSE_CX=

# OpenAI API key (server-side, used by Vite dev middleware for Realtime ephemeral tokens)
OPENAI_API_KEY=

# Telegram bot token (server-side, used by Vite dev middleware)
TELEGRAM_BOT_TOKEN=

# Text chat model for /api/openai/text (server-side)
# Example values: gpt-4o-mini (default), gpt-5-mini (if available to your account)
OPENAI_TEXT_MODEL=
